{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "yarn install --frozen-lockfile && yarn workspace @chess/frontend build",
  "installCommand": "yarn install --frozen-lockfile",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs"
}
